# 2026-04-14 工作日志

## Squid ACL Dashboard 项目

### 今日完成

1. **修复外网访问问题**
   - Gunicorn 绑定地址从 `127.0.0.1:5001` 改为 `0.0.0.0:5001`
   - 原因：127.0.0.1 只接受本地请求，外网无法访问

2. **更新一键安装脚本 (install.sh)**
   - 修改 Systemd 服务配置，使用 `0.0.0.0:5001`
   - 新增 `configure_firewall()` 函数，自动检测 ufw 状态并开放 5001 端口
   - 安装完成提示中增加防火墙配置提醒

3. **更新 README.md 文档**
   - 新增**防火墙配置**章节，包含：
     - ufw 本地防火墙配置
     - 云服务器安全组/防火墙配置说明
   - 完善**排障指南**：
     - 新增"无法访问管理面板"的详细排查步骤
     - 列出常见原因（服务未启动、端口未监听、防火墙拦截、端口冲突）
   - 手动安装步骤中更新绑定地址
   - 添加常用邮箱 SMTP 设置参考表

### 文件变更
- `install.sh` - 新增防火墙自动配置，修改 Gunicorn 绑定地址
- `README.md` - 新增防火墙配置章节，完善排障文档

### 已完成（续）

4. **修复忘记密码邮件发送状态显示 Bug**
   - 问题：邮件发送失败时，前端仍显示"邮件已发送"
   - 原因：前端模板显示固定文本，没有根据实际发送结果显示状态
   - 修复：
     - 后端 `forgot_password()` 函数添加 `email_sent` 标志传递给前端
     - 创建 `templates/forgot_password.html` 模板，根据发送状态显示不同消息：
       - `email_sent=True`：显示"邮件已发送"成功消息
       - `email_sent=False`：显示"邮件发送失败"错误消息，并允许重新发送
       - `email_sent=None`：显示表单

### 文件变更
- `app.py` - 修复忘记密码逻辑，添加 email_sent 状态标志
- `templates/forgot_password.html` - 新建模板，正确处理邮件发送状态
- `README.md` - 添加 bug 修复说明到排障指南

5. **创建命令行工具**
   - `reset_password.py` - 密码重置工具：
     - root 用户可通过命令行重置任意用户密码
     - 支持列出所有用户
     - 密码长度检查和安全提示
     - 详细的操作结果反馈
   
   - `update.sh` - 系统更新工具：
     - 从 GitHub 自动拉取最新代码
     - 自动备份当前版本（保留最近10个）
     - 更新失败自动回滚
     - 保留数据库和配置文件
     - 支持强制更新、仅备份、恢复备份等选项

### 新增文件
- `reset_password.py` - 密码重置工具
- `update.sh` - 系统更新工具
- `templates/forgot_password.html` - 忘记密码页面模板

### 待办
- 将更新后的文件推送到 GitHub
